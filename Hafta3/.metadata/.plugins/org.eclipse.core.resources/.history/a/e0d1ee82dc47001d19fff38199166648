package overriding;
public class BaseKrediManager {
    /*Mesela bu metodun ezilmesini engellemek istiyorum çünkü herkes için aynı dersek o zaman final 
	anahtar kelimesini kullanıyoruz. */
    public double hesapla(double tutar) {
        return tutar * 1.18;
    }
}
